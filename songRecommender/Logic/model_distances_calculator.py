from songRecommender.models import Song, Distance, Distance_to_List, Distance_to_User


#calculates the distance of the given song to every user in database
def save_user_distances(distances, song, distance_type):
    return

#calculates the distance of given song to every list in the database
def save_list_distances(distances, song, distance_type):
    return